# rust wasm

## command(Unverified)
```bash
# build rust wasm
cargo install wasm-pack
cd rust-wasm-hello/hello-wasm
wasm-pack build

cd ../

# prepare yarn
yarn install

# listen request
yarn webpack-cli serve
```
